/* URLhren  */
var URLhren = location.protocol+"//"+location.host+"/";
//console.log(URLhren);
var webUrl = location.search;
var theRequest = new Object();
	if (webUrl.indexOf("?") != -1) {
		var str = webUrl.substr(1);
		strs = str.split("&");
		for (var i = 0; i < strs.length; i++) {
		    theRequest[strs[i].split("=")[0]] = unescape(strs[i].split("=")[1]);
		}
	} else {
		theRequest.id = 0;
		theRequest.pid = 0;
	}
var id = theRequest.id;
var pid = theRequest.pid;


/* isLogin */
var userid = sessionStorage.getItem('userId');
var usertoken = sessionStorage.getItem('userToken');
var userpid = sessionStorage.getItem('userPid');
var xmPid = 1;
if(!myurl){
	var myurl = '';
}

console.log(myurl=="print");
//alert(userid +","+ usertoken)
if(userid === null || typeof(userid) == "undefined" || userid == "" || userid == 0 || usertoken === null || typeof(usertoken) == "undefined" || usertoken == "" || usertoken == 0 ){	
	if(myurl != "print"){
		location.href = "login.html"+htmlVtimea+"";
	}
	
}
if(userpid !== null || typeof(userpid) != "undefined" || userpid != "" || userpid != 0){	
	xmPid = parseInt(userpid);
	userpid = "&pid="+userpid+"";
}else{
	userpid = "";
	xmPid = 1;
}

/* token */
function ajaxtoken(request){
	console.log(userid +","+ usertoken);
	request.setRequestHeader("id", ""+userid+"");
    request.setRequestHeader("token", ""+usertoken+"");
}



